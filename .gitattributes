# 기본: 텍스트 파일은 자동 감지
* text=auto

###########################
# Unity 에셋들 - 줄바꿈 건드리지 말기
###########################

# YAML 기반 에셋들 (Unity가 자체 포맷으로 관리하므로 Git이 줄바꿈 안 건드리게)
*.meta   -text
*.unity  -text
*.prefab -text
*.anim   -text
*.asset  -text

###########################
# 스크립트 / 코드 파일
###########################

*.cs   text
*.js   text
*.shader text
*.cginc text

###########################
# 파이썬 스크립트
###########################

*.py text

